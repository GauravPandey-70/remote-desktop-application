{
  "name": "@desklink/common",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for DeskLink",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "lint": "echo \"lint not configured yet\""
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "rimraf": "^6.0.0"
  }
}
